
const initialState = {
  test: [
    {
      id: 'test',
      title: 'test'
    }
  ]
}

export const  testReducer = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state
  }
}
